import { createTheme } from '@sanity/ui';
import './plugins/custom-studio-styles/studio.css'; // Import the custom CSS

// Define a custom theme object
export const myCustomTheme = createTheme({
  // Colors, fonts, etc. can be customized here if needed.
  // The current setup ensures the CSS is loaded.
  // This is a placeholder for a full theme definition.
  // The actual theme object structure is complex, but importing CSS here
  // is a common pattern when creating custom themes.
});
